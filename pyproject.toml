[tool.pytest.ini_options]
# Lets `import app...` resolve from the repo root when running `pytest` from
# anywhere inside it, without needing an installed/editable package.
pythonpath = ["."]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
